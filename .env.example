PORT=3210
HOST=0.0.0.0
# Em produção atrás do proxy do Coolify:
NODE_ENV=production
ALLOW_REMOTE_HOST=1
# Opcional: origem pública exata, sem barra final.
PUBLIC_ORIGIN=
# Integração opcional; nunca coloque a chave no frontend.
OPENAI_API_KEY=
# Informe um modelo da sua conta com suporte a Responses e Structured Outputs.
OPENAI_MODEL=
